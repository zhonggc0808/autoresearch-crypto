{
  "name": "exp_0012_frozen",
  "version": "2.1",
  "description": "Phase 3B best candidate — global L250 + ADX gate on NEUTRAL+BEAR",
  "source_candidate": "research_workspace/candidates/exp_0012.json",
  "baseline": "v2.1 balanced",
  "checkpoint": "checkpoints/channel_breakout_exp_0012.pt",
  "parameters": {
    "entry_lookback": 250,
    "min_hold_bars": 432,
    "regime_filter": { "fast_days": 50, "slow_days": 200 },
    "regime_change_policy": "permission_based"
  },
  "runtime_filters": [
    {
      "id": "adx_gate",
      "type": "adx_gate",
      "description": "Force flat when ADX < 20 on NEUTRAL and BEAR regimes",
      "threshold": 20,
      "apply_to": ["NEUTRAL", "BEAR"],
      "action": "force_flat",
      "enabled": true
    }
  ],
  "expected_metrics_1300d_oracle": {
    "oos_return_pct": 183.8,
    "oos_max_drawdown_pct": -33.8,
    "oos_sharpe": 2.69,
    "oos_win_rate_pct": 53.1
  },
  "changes_from_baseline": {
    "entry_lookback": "global 375→250",
    "runtime_adx_gate": "added on NEUTRAL+BEAR, threshold=20"
  }
}
